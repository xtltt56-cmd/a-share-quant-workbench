# 前端与数据透明度升级交接（2026-09-13）

## 本轮完成

- 修复 `#import-section` 路由：从持仓页进入并定位导入区域。
- 详情随状态刷新；断线清除绿色连接样式；刷新锁使用 finally 释放。
- 实时区间位置和价位轴定位同时校验连接、交易时段、单股质量与报价时间。过期/无行情显示暂不判断。
- 日选直接显示计划适用日期、尚未到期/已过期状态和缺失原因，不把有价格计划等同于可立即执行。
- 字号调整为表格主体 14px、主要辅助文字 12px；详情自选按钮独占行，避免遮挡数据质量。
- 手机监控改为可展开卡片，保留全部 48 条；刷新后保持展开状态。修复保存折叠导航后窄屏隐藏文字的问题。
- 数据页分别显示日线截止、报价缓存时间、后台检查时间、页面读取时间及本次连接统计。
- 新增 `/api/models/progress`：复用现有前瞻账本解析与完整性校验，提供累计预测数、等待到期数、到期待结算数、已结算数、模型版本数和最近预测时间。
- 账本读取设置 16MiB 在线上限；进度读取禁止账本尾部自动恢复，损坏时返回不可用，不修改预测证据。

## 已确认的真实运行问题

本次启动为 network 模式。调度器在非 OPEN 时段直接返回 requested=False；服务随后显示恢复的缓存。`.runtime/realtime/quotes.json` 最后写入为 2026-08-18，当前启动遥测没有成功报价或请求失败。已证明本次休市启动没有向数据源请求行情；不能据此推断过去各个交易日的失败原因，也没有证明下一交易时段一定恢复。未绕过交易时段和质量限制、未伪造实时行情。

前瞻账本校验后读出 10 条预测、0 条结算、10 条到期待结算、1 个模型版本。最近预测时间为 2026-08-21；已存结算状态记录当时为 NO_MATURED_PREDICTIONS。这不是当前模型成熟的证据。历史任务和冻结比赛配置需要单独调查续接，不能通过重置比赛或补写成功结果处理。

## 验收

- 前端资源、人工投顾、券商文件 HTTP 相关测试：26 passed。
- 新增进度汇总测试与前瞻账本回归：27 passed。覆盖到期未结算不计成功、损坏尾部不截断、缺少账本时不创建文件。
- 浏览器脚本 `output/playwright/upgrade-review.cjs`：导入实际点击、390px 无横向溢出、48 张卡片、展开状态保持、详情随刷新、模拟断线与恢复通过。无 pageerror；故障注入产生的网络失败日志属于预期。
- JS 语法、相关 Python Ruff、Git diff 格式检查。
- 截图位于 `output/playwright/v21-*.png`。测试只扫描/读取数据和改变隔离浏览器状态，没有确认成交或导入。

## 使用及范围

本地服务已通过项目启动器更新加载；现有浏览器标签按 Ctrl+F5。无需新增快捷方式、账号或依赖。本轮未上传 GitHub，未新增开机启动。所有新增项目文件与截图位于 D 盘。

本轮完成界面、状态同步、前瞻进度展示和行情停滞诊断；实时报价恢复、到期预测结算恢复尚未得到实盘时段验收，不能将界面验收当作系统整体成熟或收益有效性验证。
