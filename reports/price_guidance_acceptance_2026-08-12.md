# 价格指导工作流验收记录（2026-08-12）

## 验收范围

本验收覆盖冻结的日选价格计划、持仓价格计划、盘中只读叠加、研究级数量约束和异常 fail-closed 行为。它不把规则排序当作经过校准的收益预测，也不宣称正式模型已经成熟。

## 结果

- 状态：PASS（纸面工作流）
- 日选计划：由本地日线数据生成，研究级建议数量固定为 0
- 持仓计划：由同一真实日线输入生成，独立于日选计划
- 边界稳定性：保存并重新读取后，买入区间、最高可接受价、失效价和保护价未漂移
- 盘中失败关闭：行情质量不是 GOOD、价格缺失或时间戳缺失时返回 `NO_RELIABLE_GUIDANCE`
- 人工执行：始终为真；代码中不存在下单能力

## 可复现实证

```powershell
D:\量化交易\.venv\Scripts\python.exe -m pytest tests/test_price_guidance_acceptance.py -q
```

结果：`1 passed`。

完整验收仍需在用户本机已有的真实 BaoStock/AKShare 日线湖上运行；当前仓库不使用 fixture 填充生产页面。若本地历史数据不足，系统会显示数据不足并保持数量为 0。

## 未通过的发布门槛

`config/production_readiness.yaml` 中的正式模型、实时数据稳定性、券商只读连接和整体人工投顾仍保持 BLOCKED。要解除这些阻断，必须分别取得足够的真实历史/成熟样本、连续实时质量证据、财信 QMT 只读授权，以及经人工批准的模型治理记录。
