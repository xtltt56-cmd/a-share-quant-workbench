# A 股量化交易工作台交接报告

生成日期：2026-08-11  
运行模式：`PAPER_ONLY`（纸面监控、人工执行）  
项目目标：面向财信证券 A 股账户的日选、盘中监控、人工成交登记与人工复核建议。

## 一、当前可用结论

当前版本已经可以启动本地工作台，并且“官方日线候选”和“盘中监控”不会因为实时源暂时不可用而空白：

- 官方日选：已从本地真实 BaoStock 日线生成 10 条候选，信号日期为 2026-08-07。
- 盘中监控：启动时先展示这 10 条候选的 `STALE_DATA` 监控行；实时行情通过质量核验后才覆盖价格和状态。
- 人工投顾页：支持持仓回放、人工成交四字段登记（名称、代码、数量、价格）、预览/确认两步记录。
- 自动交易：没有委托、撤单或自动报单路径；所有建议都标记为仅供人工执行。
- 当前日选是固定权重横截面“排名”，不是经过样本外校准的收益概率预测。因此没有把排名伪装成“买入/卖出”结论；没有经核验上下文时，人工投顾页明确显示“暂不操作”。

## 二、已完成的主要工作

### 数据与日选

1. 增加 BaoStock 历史日线适配器和 CSI300（`000300` → `sh.000300`）指数读取。
2. 生产日选只接收来源为 `baostock` 的规范化日线，拒绝 fixture、回放、合成和测试数据。
3. 生产门槛固定为：单标的至少 252 个交易日、共同日期横截面至少 30 只股票、20 日平均成交额至少 1000 万元、收盘价高于 60 日均线。
4. 使用现有规则因子引擎，不另造未经验证的模型；估值因子在免费数据阶段禁用，避免把价格倒数误当估值。
5. 生成结果以原子方式写入 `.runtime/signals/official-daily.json`，含数据截止日、模型/特征版本、排名理由、参考价格和失效价格。

当前候选（仅排名，不构成买卖指令）：

| 排名 | 代码 | 名称 | 分数 | 参考价 |
|---:|---|---|---:|---:|
| 1 | 000831 | 中国稀土 | 80.96 | 57.19 |
| 2 | 601899 | 紫金矿业 | 78.80 | 35.15 |
| 3 | 603228 | 景旺电子 | 76.80 | 94.95 |
| 4 | 300759 | 康龙化成 | 73.81 | 45.28 |
| 5 | 603259 | 药明康德 | 72.97 | 154.82 |
| 6 | 002428 | 云南锗业 | 69.79 | 100.08 |
| 7 | 601138 | 工业富联 | 67.05 | 68.18 |
| 8 | 300058 | 蓝色光标 | 65.72 | 15.88 |
| 9 | 600519 | 贵州茅台 | 62.55 | 1309.22 |
| 10 | 600276 | 恒瑞医药 | 57.13 | 54.62 |

本地数据湖实测：50 只股票加 1 个 CSI300 基准，共 56 个日线文件中筛出 BaoStock 数据 74,804 行，覆盖 2020-01-02 至 2026-08-07。数据文件位于 `data/lake/daily_bars`，属于本机运行产物，不纳入 Git。

### 实时行情与质量控制

1. 免费实时端点顺序为新浪 → 东方财富 → 腾讯；当前新浪成功时显示 `AKShare / 新浪`。
2. 全市场请求最小间隔 60 秒，并对失败端点做 300 秒冷却，避免 AKShare 高频调用触发临时封禁。
3. 腾讯备援已修正成交量（手→股）和成交额（万元→元）单位；因为缺少可靠交易所时间，腾讯只标记为降级数据，不能进入 GOOD。
4. 未来时间戳、过期报价、缺失标的和倒退时间戳均会被隔离；质量门只有连续两个不同且新鲜的快照才可进入 GOOD。
5. 实时源不可用、闭市或刚启动时，系统保留日选对应的监控行并标记 `STALE_DATA`，严禁生成可执行结论。

### 本地人工投顾与账本

- `GET /advisory` 为简体中文页面。
- 持仓来自本机 JSONL 账本回放，不冒充券商持仓。
- 人工登记只需名称、代码、数量、价格；预览不会落盘，确认后才追加账本。
- 确认令牌一次性消费，重复确认被拒绝。
- 指导建议会显示证据截止日、状态、理由和人工执行提示；没有正式预测上下文时返回 `暂不操作`。

## 三、启动和日常操作

在 `D:\量化交易` 打开 PowerShell：

```powershell
# 重新生成最新本地日选（数据目录已有 BaoStock 文件时无需联网）
.\scripts\quant_cli.py daily-candidates

# 启动工作台，允许实时公开行情；如只想离线查看日选，改用 -Offline
.\scripts\start_quant_workbench.ps1
# 或：
.\scripts\start_quant_workbench.ps1 -Offline

# 停止工作台
.\scripts\stop_quant_workbench.ps1
```

页面地址：

- 主工作台：`http://127.0.0.1:8765/`
- 人工投顾：`http://127.0.0.1:8765/advisory`

检查接口：

```powershell
Invoke-RestMethod http://127.0.0.1:8765/api/health | ConvertTo-Json -Depth 8
Invoke-RestMethod http://127.0.0.1:8765/api/state  | ConvertTo-Json -Depth 10
```

应看到 `paper_only=true`、`live_trading_enabled=false`，以及 `official_daily_candidates` 和 `intraday_monitor` 为非空数组。闭市/无实时源时监控行显示过期是预期的安全状态。

## 四、财信证券连接状态与必须的人工步骤

当前登录的“财信证券专业版”是普通同花顺组件，不是迅投 QMT；本机没有 `xtquant`、`userdata_mini` 或 QMT Python 组件。因此不能也不应通过界面抓取、OCR 或逆向方式读取账户数据。

财信官网将“财信证券专业版”和“迅投 QMT”列为两个独立软件。要使用官方只读行情/资产接口，仍需由用户完成：

1. 联系 95317 或客户经理，申请开通“迅投 QMT 量化服务和 XtQuant Python API 权限”，明确只申请行情、资产、持仓查询，不使用程序化报单。
2. 确认系统费、资产/交易量门槛，以及是否包含 Level-1 实时和历史行情；不能仅凭软件下载页推断免费无门槛。
3. 审批后从财信官方软件下载页安装迅投 QMT，登录 MiniQMT，并确认外部 Python 能找到 `xtquant` 与 `userdata_mini`。

完成上述人工步骤后，后续只实现只读桥接：SDK 路径发现、健康检查、行情适配、资产/持仓查询和失败降级；永久不暴露下单/撤单接口。

## 五、测试与审查证据

- 全量测试：`392 passed, 1 skipped, 2 warnings`。
- 跳过项是 Windows 当前没有创建文件符号链接权限，不是业务失败。
- Ruff：`All checks passed!`
- `git diff --check`：通过。
- 重点覆盖：日选真实性与门槛、信号原子持久化、未来/过期行情隔离、免费实时源单位换算、中文页面、人工账本并发确认、备份恢复、QMT 只读安全边界和启动器路径安全。

测试中的两个警告来自第三方 pandas/QLib 的弃用提示，不影响当前功能，但后续升级依赖时应复核。

## 六、尚未完成或需要后续迭代的事项

1. **官方 QMT 只读连接**：等待财信审批和用户安装/登录 QMT；普通客户端登录不能替代该权限。
2. **可校准预测**：目前只有规则排序，没有样本外收益概率、置信区间或经过走步验证的 LightGBM/双重集成冠军模型，不能承诺准确率。
3. **基本面与 PIT 数据**：免费阶段尚未建立完整公告日、财报修订和历史成分股 PIT 数据，估值/质量因子暂不启用。
4. **跨进程实时快照缓存**：当前有同进程节流和日选降级展示；重启后实时快照仍需重新核验，后续应加入带摘要校验的原子缓存，并始终标记 `CACHED/STALE`。
5. **实时调度运营**：目前工作台后台轮询已具备，但还没有生产级任务编排、告警、轮换日志和长期监控面板。
6. **交易成本与回测**：应在扩大股票池后加入滑点、印花税、佣金、涨跌停、停牌和 T+1 约束，并执行严格的时间切分/样本外回测。

这些项目完成前，系统适合作为“研究辅助 + 人工复核 + 纸面监控”，不应被描述为自动赚钱或准确预测系统。

## 七、关键文件

- `src/a_share_quant/research/daily_candidates.py`：真实日线候选生成。
- `src/a_share_quant/storage/official_signal_store.py`：官方日选持久化与校验。
- `src/a_share_quant/data/realtime/`：新浪/东方财富/腾讯实时源及质量控制。
- `src/a_share_quant/workbench/service.py`：日选与盘中监控状态汇总。
- `src/a_share_quant/workbench/advisory_service.py`：人工账本和指导建议边界。
- `src/a_share_quant/integrations/qmt/read_only.py`：QMT 只读安全边界（尚未连接真实 SDK）。
- `reports/official_daily_generation.md`：本次日选生成记录。
- `reports/realtime_data_smoke_test_current.md`：实时数据现场烟测记录。
- `reports/network_diagnostics_current.md`：脱敏网络诊断记录。

