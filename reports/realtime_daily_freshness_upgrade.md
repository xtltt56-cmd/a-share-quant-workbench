# 实时行情与日线新鲜度升级交接报告

日期：2026-08-13

## 本次完成

1. 实时行情采用“优先标的 + 全市场低频刷新”模式。官方日选、已登记持仓和导入持仓优先请求；全市场请求最小间隔为 60 秒，失败不再与后台 15 秒轮询叠加重试。
2. AKShare 优先标的使用单股公开行情接口；新浪、东方财富、腾讯按故障转移顺序提供全市场后备，腾讯成交量和成交额已换算为股、元。
3. 后台行情请求不再持有状态读取锁。即使公共行情接口慢，浏览器读取 `/api/state` 仍能及时返回。
4. 日线数据增加交易日截止日校验。启动时会用免费 BaoStock 增量更新本地日线；数据落后时保留原候选但明确标记 `STALE_DATA`/更新失败，不伪造最新推荐。
5. 工作台增加行情总数、过期行情数、最新/最旧行情时间、日线状态、日线截止日期和中文提示；“官方日线候选”和“盘中监控”都从持久化状态加载，不再因首次实时请求失败而为空。
6. 已用真实 BaoStock 数据更新本地日线至 2026-08-12，并重新生成 10 条官方日线候选。

## 实际数据位置

- 日线数据：`data/lake/daily_bars/*.parquet`
- 官方日选持久化文件：`.runtime/signals/official-daily.json`
- 实时行情缓存：`.runtime/realtime/quotes.json`
- 人工投顾/导入文件：`.runtime/advisory/`

## 验证证据

- 全量测试：`556 passed, 3 skipped`，2 个上游依赖警告（pandas/qlib），无项目测试失败。
- 重点测试（实时、调度、工作台、日线刷新、CLI）：`58 passed`。
- Ruff：`All checks passed!`
- `git diff --check`：通过。
- 运行中接口：`/api/health` 约 200ms；`/api/state` 首次约 24ms，后续约 126ms。
- 现场状态：数据源 `AKShare / Sina`；日线 `FRESH`，截止 `2026-08-12`，来源 `BaoStock`；官方日选 10 条；盘中监控 100 条。

## 当前状态的正确解释

现场盘中质量显示 `DEGRADED`，部分行情显示 `STALE_DATA`，不是页面故障，而是保护机制：免费公共行情中有些标的的交易所时间超过 60 秒，系统拒绝把混合/过期数据标成 `GOOD` 或生成可执行买卖结论。系统仍展示最新可审计报价、时间戳和数据年龄；用户必须人工复核，系统不会下单。

## 尚未完成或需要人工条件

- 财信 QMT 只读接入仍需财信审批并安装迅投 QMT/XtQuant；当前普通财信客户端不能作为受支持 API。获得权限后再实现 SDK 路径发现、只读资产/持仓查询和更可靠行情适配，仍不开放报单。
- 免费公共行情有访问频率和稳定性限制；全市场按 60 秒以上刷新是合规的保守设置。若需要更低延迟/更高覆盖，应接入已授权的 QMT、券商行情或付费数据源。
- 当前官方日选是规则/因子排序，不代表已校准的收益概率；模型进化、滚动回测、样本外评估和晋升门槛仍需持续积累真实历史数据后开展，不能承诺预测准确率。
- 系统保持纸面监控和人工执行模式，没有自动交易功能。

## 启动方式

双击桌面“ A股量化交易系统 ”快捷方式即可启动本地工作台；启动脚本会打开主监控页和“本地人工投顾”页。主监控地址为 `http://127.0.0.1:8765/`，人工投顾地址为 `http://127.0.0.1:8765/advisory`。
